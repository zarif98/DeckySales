import deckyPlugin from "@decky/rollup";

// Decky's standard build: ES module output, with React, @decky/ui and the
// JSX runtime taken from the Steam client rather than bundled.
export default deckyPlugin({});
